# Security Policy

This repository is a documentation-only public reading list. It has no runtime,
dependencies, credentials, deploy scripts, or automation state.

If you find a private token, credential, or sensitive link in this repository,
do not open a public issue containing the secret. Open a minimal issue that
describes the affected file and line, or contact the maintainer through the
profile links.
